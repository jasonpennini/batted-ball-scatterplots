import PouchDB from 'pouchdb';

// Initialize a new PouchDB database instance
const db = new PouchDB('my_database'); // Replace 'my_database' with a name that makes sense for your data

export default db;
